import { useState } from "react";
import { useTranslation } from "react-i18next";
import {
  Flex
} from "@chakra-ui/react";

function TravelsPanel() {

  return (
    <Flex>

    </Flex>
  )
}
export default TravelsPanel;